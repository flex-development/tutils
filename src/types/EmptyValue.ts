import type { NIL } from './NIL'

/**
 * @file Types - EmptyValue
 * @module types/EmptyValue
 */

/**
 * Type representing `null`, `undefined`, and empty strings.
 */
export type EmptyValue = NIL | ''
