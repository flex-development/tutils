/**
 * @file Unit Tests - dot
 * @module tutils/utils/tests/unit/dot
 */

import testSubject from '../dot'

describe('unit:utils/dot', () => {
  it('should equal "."', () => {
    expect(testSubject).to.equal('.')
  })
})
